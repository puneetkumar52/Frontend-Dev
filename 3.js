let expenses = [2000, 2500, 5000, 300, 2000];

function add(a, b) {
    return a + b;
}
let total = 0;
for (let i = 0; i < marks.length; i++) {
    total += marks[i];
}
let average = (total / expenses.length).toFixed(2);
let taxed = (total * 1.10).toFixed(2);

console.log("Total:", total);
console.log("Average:", average);
console.log("After Tax:", taxed);